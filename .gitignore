# Home directory is the repo root — ignore everything by default,
# then whitelist only what we intend to track.
/*
!/.gitignore
!/.github/
!/docs/
!/finance-tracker/

# Project instructions for coding agents. These are the same document —
# AGENTS.md is the Codex-facing copy. Edit both together, always.
!/CLAUDE.md
!/AGENTS.md
